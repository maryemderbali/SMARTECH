<form method="post" action="{{ path('app_paniers_delete', {'panierId': panier.panierId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier.panierId) }}">
    <button class="btn">Delete</button>
</form>
