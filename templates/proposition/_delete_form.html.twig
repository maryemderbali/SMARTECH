<form method="post" action="{{ path('app_proposition_delete', {'nomPrestataire': proposition.nomPrestataire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposition.nomPrestataire) }}">
    <button class="btn">Delete</button>
</form>
