<form method="post" action="{{ path('app_type_produit_delete', {'idt': type_produit.idt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_produit.idt) }}">
    <button class="btn">Delete</button>
</form>
